# 04_Development_History — 開発履歴と現状のTODO

---

## 1. 開発履歴（概要）

Project Pack（`03_Development_History.md`）記載の情報および、本セッションで
確認できた範囲のサマリ。

| フェーズ | 内容 |
|---|---|
| RC1 | ブランディング、KPI修正、カレンダー修正、本番環境移行 |
| RC2 | HOME再設計、イベント再設計 |
| v1.1 | 誕生日機能・各種修正（`012_v1.1_birthday_and_fixes.sql`）、
  イベント機能（`013_v1.1_rc2_events.sql`）、オフライン対応基盤
  （第38章：`OfflineBanner`, `SyncManager`, `CustomerCacheSyncManager`,
  `src/lib/offline/`配下のIndexedDBキュー等）を導入 |
| RC4〜RC9 | 同伴者・誕生日連携、検索拡張（トライグラム検索、同伴者・
  ボトル名検索対応）、監査ログ、集計・RLS強化など、`supabase/007〜011`,
  `014〜017`の各マイグレーションに対応する機能追加・不具合修正 |
| v1.2 | 「シンプルさ最優先（Simplicity-first UX）」を方針に掲げた
  UI再設計フェーズ。詳細は「2. v1.2セッションでの対応履歴」参照。 |
| v1.3 | カレンダー連携機能「＋カレンダーに反映させたいイベント」の追加。
  詳細は「3. v1.3セッションでの対応履歴」参照。 |
| v1.4 | 「❔使い方ガイド」機能の追加（共通ヘッダーボタン・モーダル表示・
  画面別ガイド紐付け）。詳細は「4. v1.4セッションでの対応履歴」参照。 |

---

## 2. v1.2セッションでの対応履歴（時系列）

以下は今回のClaudeとのやり取りで対応した内容の時系列サマリ。
各回のレポートは別途zipで納品済み（ファイル名は概要欄参照）。

### 第1回：HOME・カレンダー・お知らせ・設定・顧客ページの基礎修正
- HOME日付表示の重複解消、カレンダー表示項目の整理（来店・予約・誕生日・
  スタッフ休みのみ）、検索のインライン化、顧客ページの初回来店表示修正等。
- 未使用コード（`src/lib/data/events.ts`内の`buildNoticeItems`、
  `src/components/dashboard/DashboardSearch.tsx`）を削除。

### 第2回：パフォーマンス改善＋HOME/お知らせ/設定/ボトル検索の追加修正
- **最大の変更**：ログイン後の全ページを`src/app/(app)/`ルートグループへ
  移動し、共通レイアウト（`layout.tsx`）でヘッダー・ナビ・プロフィール
  取得を一元化。画面遷移の体感速度改善が目的。
- カレンダーの曜日計算バグ（`getDay()`→`getUTCDay()`）修正。
- 来店編集機能（`/visits/[id]/edit`）を新規追加。
- **この回でVercelのビルドエラーが複数回発生**（後述「5. 既知の
  ハマりどころ」参照）。最終的に全ファイルを1つの自己完結パッケージに
  まとめて解決。

### 第3回：カレンダー顧客名リンク化・来店編集機能の追加
- カレンダー「選択日の詳細」パネルの顧客名を`/customers/[id]`へリンク化。
- 来店の「修正」「無効化」機能を顧客詳細ページ・カレンダー両方から
  アクセスできるように。

### 第4回：集計バグ・スタッフ休み表記・イベント絵文字バグの本格修正
- HOME「本日の売上合計」が¥0になる／全日付に同じ金額が出て見える
  バグを、カレンダーの選択日と連動する設計に変更して解消。
- スタッフ休み表記「🐑休」（赤字の休）をHOME・お知らせ・設定すべてで
  統一。過去のテストデータ（🐑🚫・スタッフA表記）は表示側の正規化で解消
  （DB変更なし）。
- イベント絵文字自動付与（📅）バグを、新規保存時・既存データ表示の
  両方について解消。
- カレンダー日付マスのインジケーター表示順を固定（来店が最優先で
  上に来るように）。
- 顧客詳細ページの「この顧客を無効化」ボタンを削除。

### 第5回：設定ページの細部修正
- スタッフ休み登録ボタンから「スタッフA」等の文字表記を完全に削除。
- イベント一覧の「編集」ボタンがクリックしても反応しないように見える
  不具合を修正（フォームへの自動スクロール追加）。

### 第6回：設定ページのイベント管理をさらに整理
- イベント一覧の表示件数に上限がないことを確認・保証。
- スタッフ休み一覧を「📁 スタッフ休み一覧（◯件）」の折りたたみフォルダに
  分離し、通常イベント一覧を圧迫しないように整理。

### 第7回：カレンダー月合計フッターの削除＋検索ワード「月別売上」の追加
- カレンダー最下部に常設していた「◯月の売上合計」フッター（今月以外の
  月を表示中だけ出ていたもの）を削除（「見えすぎる」との意見を反映）。
- HOME検索欄に完全一致で「月別売上」と入力したときだけ、カレンダー下部に
  対象年（カレンダーの年月ナビで表示中の年）の1〜12月の売上内訳＋年合計
  を表示する仕様に変更。通常の顧客横断検索とは別の分岐として実装し、
  該当ワード入力時は顧客検索は実行しない。
- 新規データ取得関数 `getYearlyMonthlyAmounts(year)`
  （`src/lib/data/dashboard.ts`）を追加。既存の`getMonthSummary`の
  `monthTotalAmount`/`monthTotalTip`フィールド自体は変更していない
  （参照している箇所がなくなっただけで、DB問い合わせ内容も無変更）。

**v1.2の間、DBスキーマ変更・SQLマイグレーションの追加は一度も発生していない。**

---

## 3. v1.3セッションでの対応履歴（時系列）

### 第1回：カレンダー連携機能「＋カレンダーに反映させたいイベント」の追加
- **背景**：従来、イベントを登録しても「MATTYからお知らせ」ページにのみ
  表示され、カレンダー本体（グリッド・選択日の詳細）には一切反映されない
  仕様だった。「同伴」「遅番」「●●を渡す」等の予定をカレンダー上で直感的に
  確認したいという要望に対応。
- **DBスキーマ変更は行っていない**：`store_events`テーブルの`event_type`
  カラムは、`013_v1.1_rc2_events.sql`時点でCHECK制約のないtext型として
  定義されているため、新しい値`"calendar"`を追加するだけで対応できた。
  `title`/`emoji`/`memo`/`start_date`/`end_date`等の既存カラムをそのまま
  流用しており、マイグレーションSQLの追加・実行は発生していない。
- 設定ページ（イベント管理）：「＋ 新しいイベントを追加」ボタン・一覧の
  **直下**に「＋カレンダーに反映させたいイベント」セクションを新設。
  タイトル・専用の絵文字入力欄・日付・メモを入力する独立フォーム
  （日程タイプは単発日のみに簡略化）。タイトル・絵文字はともに必須
  バリデーションを追加（絵文字が空のまま保存されるとカレンダー上に
  何も表示されなくなるため）。通常イベント一覧・スタッフ休み一覧とは
  別の専用一覧を新設し、編集・削除に対応（上限なし・全件表示）。
- カレンダーグリッド（`CalendarClient.tsx`）：登録した絵文字のみを
  日付マスへ表示（**件数上限なし**。複数件は折り返し表示し、マスの
  高さが可変になる）。既存のインジケーター表示順（①来店②予約③スタッフ
  休み④誕生日）の後に⑤として追加し、既存表示の順序・位置は変更していない。
- 選択日の詳細パネル：新セクション「📅 予定」を追加し、タイトル全文と
  メモを表示。URLが登録されていればタイトルがリンクになる（既存の
  一般イベントと同じ挙動）。
- 「MATTYからお知らせ」ページ（`/calendar`）：コード変更なしで、新イベント
  も既存の一般イベントと同じ枠に自動的に表示される（お知らせページ側の
  クエリが`event_type !== "staff"`のみで判定する実装だったため）。ユーザー
  確認の結果、この「重複表示」は意図した挙動として採用。
- **修正ファイル**：
  - `src/lib/data/events.ts`（`resolveEventDatesForMonth`の返却値に
    `memo`を追加）
  - `src/lib/data/calendar.ts`（`CalendarDayData`に`calendarEvents`
    フィールドを追加し、`event_type === "calendar"`のイベントを専用に
    振り分け）
  - `src/components/calendar/CalendarClient.tsx`（グリッドへの絵文字
    表示、詳細パネルへの「📅 予定」セクション追加）
  - `src/components/settings/EventManagePanel.tsx`（新セクション・
    新フォーム・新一覧を追加）
  - `src/lib/actions/events.ts`（`saveStoreEventAction`）は**変更なし**
    （既存の汎用引数（`event_type`・`memo`等はすべて任意項目として
    実装済み）でそのまま対応できたため）。

**この回、DBスキーマ変更・SQLマイグレーションの追加は発生していない。**

---

## 4. v1.4セッションでの対応履歴（時系列）

### 第1回：「❔使い方ガイド」機能の追加
- **背景**：`public/guide/`配下に、操作手順を解説する静的HTMLガイド
  （目次＋各操作ページ、19ファイル）がすでに用意されていた。しかし
  MATTYはPWAとして同一ドメイン・同一スコープでインストールされるため、
  `/guide/index.html`をURLから直接開くと、ガイドではなくMATTY本体が
  開いてしまう場合があった。外部ブラウザ・新しいタブに依存せず、MATTY
  本体の画面内でガイドを確認できるようにするための機能追加。
- **調査フェーズ**：実装前に、共通ヘッダー（`Header.tsx`/`AppShell.tsx`）、
  設定画面のイベント管理コンポーネント（`EventManagePanel.tsx`。スタッフ
  休み登録も同一ファイル内に実装されていることを確認）、
  `public/guide/pages/`の実ファイル名（推測せず全件確認）、各画面と
  ガイドページの対応案を調査・報告してからユーザー承認を得て実装した。
  調査時点では「＋カレンダーに反映させたいイベント」専用のガイドページ
  （`19-calendar-event.html`）が存在せず、ユーザー側で別途作成・
  `repomix-output.xml`更新が行われたため、実装直前に再確認している。
- 新規コンポーネント（`src/components/guide/`）：
  - `HelpGuideModal.tsx`：ガイドページを`<iframe>`で表示するモーダル
    本体。タイトル「MATTY 使い方ガイド」、右上「×」、下部「閉じる」
    ボタン、モーダル外クリック、Escキーいずれでも閉じられる。開いている
    間は`document.body`のスクロールを抑制し、**閉じるとコンポーネント
    ごとアンマウントして`<iframe>`を破棄する**（背景に重い要素を
    残さない設計。Android 7.0のような非力な端末（dtab Compact d-01J等）
    での動作を考慮し、`backdrop-filter`等の重いCSSは使用していない）。
  - `HelpGuideButton.tsx`：「❔」ボタンと`HelpGuideModal`をまとめた
    再利用コンポーネント。`variant="header"`（共通ヘッダー用。スマホ幅
    ではラベルを隠し❔アイコンのみ表示）と`variant="inline"`（設定画面
    など特定の項目そばに置く小さいボタン。常にラベル表示）の2種類。
    `guideUrl`を指定しない場合は現在のパスから自動でガイドURLを解決し、
    指定した場合は常にそのURLを開く。
- 新規ファイル `src/lib/guide/guide-links.ts`：ガイドURLを1箇所で管理する
  対応表（`GUIDE_URLS`）と、パス→ガイドURLの解決関数
  （`getGuideUrlForPath`）。各画面へURL文字列を直接書かない方針を徹底する
  ため新設（01_System_Spec.md「5. 主要な設計原則」8番参照）。
- `src/components/layout/Header.tsx`：ログイン後の全主要画面で共通描画
  されるヘッダーに、「❔使い方」ボタン（`HelpGuideButton variant="header"`）
  をログアウトボタンの手前に追加。日付バー・スタッフ名表示・ログアウト
  機能への変更はない。
- `src/components/settings/EventManagePanel.tsx`：設定画面のイベント管理
  内、3箇所に固定ガイドを開く「❔」ボタン（`variant="inline"`）を追加。
  フォーム・保存処理・一覧表示ロジックへの変更はない。
  - 「スタッフ休みを登録」見出し横 → `12-staff-off.html`
    （スタッフの休みを登録する）
  - 「＋ 新しいイベントを追加」の隣 → `18-event.html`
    （おまけ：お知らせに表示するイベントを登録してみよう）
  - 「＋カレンダーに反映させたいイベント」の隣 → `19-calendar-event.html`
    （おまけ②：カレンダーに反映させたいイベントを登録しよう）
- ガイドページと画面の対応（`guide-links.ts`の`PATH_GUIDE_MAP`。共通
  ヘッダーの「❔使い方」が現在のパスに応じて開くガイド）：
  - `/search`（顧客検索）→ `/guide/pages/06-search.html`
  - 上記に該当しない画面（HOME・来店登録・顧客一覧・予約・お知らせ・
    ボトル・設定全体など）→ `/guide/index.html`（目次）を開く
    （対応ページの無い画面は目次を開く、という仕様どおりの既定動作。
    ユーザー確認済み）。
- **DBスキーマ変更は行っていない**：フロントエンドのみの機能追加。

**修正・新規ファイル：**
- 新規：`src/lib/guide/guide-links.ts`
- 新規：`src/components/guide/HelpGuideModal.tsx`
- 新規：`src/components/guide/HelpGuideButton.tsx`
- 修正：`src/components/layout/Header.tsx`
- 修正：`src/components/settings/EventManagePanel.tsx`

**この回、DBスキーマ変更・SQLマイグレーションの追加は発生していない。**

---

## 5. 既知のハマりどころ（今後同じ轍を踏まないための記録）

1. **ルートグループ導入時のフォルダ重複によるビルドエラー**：
   `src/app/dashboard/`等の旧フォルダを削除せずに`src/app/(app)/dashboard/`
   を追加すると、Next.jsが同一URLに対応する2つのページを検出して
   ビルドエラーになる。フォルダ構成を変える変更を渡す際は、削除すべき
   旧フォルダを明示すること。
2. **複数回に分けて渡したzipの手動マージによる部品ファイル欠落**：
   ページ側の変更だけが先に反映され、それが依存するコンポーネント
   （例：`VisitEditForm.tsx`）が未反映のままデプロイされ、
   「Module not found」エラーになった。**複数ファイルにまたがる新機能は、
   できる限り1つの自己完結したパッケージにまとめて渡す**ことで解決した。
3. **Server Actionsファイルへの型・定数の混入**：
   `"use server"`ファイルに`UpdateVisitState`型と初期値定数を直接置いたら
   Next.js 16のビルドルール違反になった。既存の`xxx-state.ts`分離パターンを
   踏襲することで解決（03_AI_Guidelines.md参照）。
4. **曜日計算のタイムゾーン依存バグ**：日付文字列から生成した
   Dateオブジェクトに対し`getDay()`（ローカルタイムゾーン依存）を使うと、
   サーバーの実行環境によって曜日が1日ズレることがある。
   `getUTCDay()`への統一で解決。
5. **表示側の正規化 vs DB修正の判断**：絵文字自動付与バグのような
   「過去データに残った不整合」は、影響範囲・リスクを鑑みてまず
   表示側（データ取得関数）での正規化を優先し、DBへの書き込みを
   伴う修正は行わない、という判断を一貫して採用した。
6. **既存カラムの区分値追加でDB変更を回避（v1.3で確立）**：新しいイベント
   カテゴリが必要になった際、テーブル・カラムを増やす前に、まず
   既存の区分カラム（`event_type`等）にCHECK制約があるかを確認する。
   制約がなければ新しい値を追加するだけでマイグレーション不要になる
   ケースがある（`event_type = "calendar"`追加の例）。逆に制約がある
   場合は、値追加であってもマイグレーションが必要になるため要確認。
7. **静的ガイド（public/guide/）とAI側コンテキストのズレ（v1.4で発生）**：
   実装前調査の時点では`public/guide/pages/`に「おまけ②」
   （`19-calendar-event.html`）が存在せず、ユーザーへ確認したところ
   「実際には作成済みで、`repomix-output.xml`側が未反映だった」ことが
   判明した。`public/guide/`のような静的アセットは、ソースコードの
   `repomix-output.xml`と同様に**実ファイルが正**であり、AI側の
   コンテキスト（過去の調査結果を含む）が古い可能性を常に疑うこと。
   ファイル名を推測せず、都度実ファイルで確認する運用を徹底した。

---

## 6. 既知のバグ・未解決事項（現時点）

1. **来店編集機能は同伴者・タグの変更に対応していない**：
   `/visits/[id]/edit`では、来店日時・金額・チップ・支払方法・席タイプ・
   領収書・メモのみ修正可能。同伴者やタグを直したい場合は、現状
   「無効化 → 再登録」で対応する必要がある。対応するには、既存の
   `create_visit_with_details` RPCの拡張、または多対多更新を安全に扱う
   専用の仕組みが必要（02_Database_Schema.md 2.4節、
   03_AI_Guidelines.md 3節参照）。
2. **未使用コンポーネントの残存**：
   - `src/components/dashboard/MonthlyLineChart.tsx`：どこからも
     importされていない（`DashboardSearch.tsx`削除時に発見、
     明示的な削除指示がなかったため残置）。
   - `src/components/customers/HideCustomerButton.tsx`：顧客詳細ページから
     使用箇所を削除した結果、参照されなくなった（機能自体を削除する
     指示ではなかったため、コンポーネント自体は残置）。
   実害はないが、次回の整理タイミングでまとめて削除するのが望ましい。
3. **設定ページ：スタッフ休み登録ボタンのツールチップ**：
   PC・マウス操作時のみ、ボタンにカーソルを乗せるとブラウザ標準の
   ツールチップで「スタッフA」等が一瞬表示される（`title`属性）。
   タップ操作（スマホ・タブレット）では表示されない。完全に消したい
   場合は次回対応可能。
4. **画面遷移パフォーマンスの限界**：
   共通レイアウト化・認証確認の重複排除・軽量ローディングへの変更で
   体感速度は改善したはずだが、Supabaseへの実際のデータ取得時間
   （ネットワーク往復時間）自体は変えていない。`force-dynamic`を
   維持しているため、キャッシュを使った更なる高速化の余地はある
   （ただし過去に「反映されない」バグの原因になった経緯があるため、
   `revalidatePath`呼び出し漏れの洗い出しとセットで慎重に検討する必要がある）。
5. **オフライン対応の現状**：
   `OfflineBanner`・`SyncManager`・`CustomerCacheSyncManager`・
   `src/lib/offline/`（IndexedDBキュー・オンライン状態フック等）といった
   基盤コードは既に存在している（v1.1時点の「第38章」対応と思われる）。
   ただし本セッションではこれらの機能を深く検証・修正の対象にはして
   いないため、**実際にオフライン→オンライン復帰時の同期が現状どこまで
   正しく動くかは未検証**。次のTODOとして動作確認・必要なら改修を行うのが
   よい。
6. **カレンダー反映イベントの表示件数（v1.3で追加した仕様）**：
   ユーザー確認の結果、日付マスに表示する絵文字の件数は意図的に
   上限なしとしている（スタッフが3名のため、スタッフ休みの最大2件表示
   との整合は問題ないと判断済み）。ただし今後、1日に大量のカレンダー
   反映イベントが登録された場合、マス目の高さが大きく伸びる可能性が
   ある。運用してみて見づらさが問題になった場合は、上限＋「他◯件」
   表示等への変更を検討する。
7. **使い方ガイド：専用ページが無い画面は目次表示のみ（v1.4で追加した仕様）**：
   来店登録・顧客一覧・予約・ボトル・設定全体など、`06-search.html`
   （顧客検索）以外の画面では専用ガイドページが無いため、共通ヘッダーの
   「❔使い方」は目次（`/guide/index.html`）を開く（ユーザー確認済みの
   意図した挙動）。該当する専用ガイドページが`public/guide/pages/`へ
   追加された場合は、`src/lib/guide/guide-links.ts`の`PATH_GUIDE_MAP`へ
   1行追記するだけで、その画面専用の遷移に切り替えられる。

---

## 7. 次にやること（TODO候補）

優先度は目安。ユーザーとの再確認を推奨。

### 高
- [ ] オフライン対応（同期キュー・復帰時の挙動）の動作検証と、
  必要に応じた改修。
- [ ] 来店編集機能への同伴者・タグ編集対応（多対多更新の設計を含む）。

### 中
- [ ] 未使用コンポーネント（`MonthlyLineChart.tsx`, `HideCustomerButton.tsx`）
  の削除、またはコードベースからの正式な棚卸し。
- [ ] スタッフ休み登録ボタンのツールチップ（`title`属性）除去。
- [ ] `force-dynamic`のうち、更新頻度の低い参照系データ（祝日一覧等）に
  限定してキャッシュを導入できないかの検討（画面遷移の更なる高速化）。
- [ ] カレンダー反映イベントの1日あたり表示件数について、運用してみて
  多くなりすぎる場合は上限＋「他◯件」表示等の検討（現状は上限なし仕様
  として確定・実装済み。あくまで運用結果次第の任意対応）。

### 低・任意
- [ ] `store_events`テーブルの過去データ（スタッフ休みの`title`/`emoji`、
  📅自動付与分の`emoji`）を、表示側正規化に頼らずDB側でも
  クリーンアップするSQLの実行（02_Database_Schema.md 2.3節にサンプルSQL
  あり。実行は任意・要バックアップ）。
- [ ] 来店登録・顧客一覧・予約・ボトル等、`06-search.html`以外の画面向けの
  専用使い方ガイドページを`public/guide/pages/`へ追加し、
  `guide-links.ts`の`PATH_GUIDE_MAP`に対応を追記する（現状は目次表示のみ。
  運用してみて個別ガイドの必要性が高い画面があれば対応）。

---

## 8. ドキュメント運用について

このファイル（および01〜03）は、v1.4時点の一連の修正が完了した時点の
スナップショットです。今後新しい機能追加・不具合修正を行う場合は、
このドキュメントセットも合わせて更新することを推奨します
（特に本ファイルの「4. v1.4セッションでの対応履歴」に続けて新しい回を
追記する、または新しいメジャーな変更がある場合は「5. v1.5セッションでの
対応履歴」のように節を追加し、既知のハマりどころ・既知のバグ・未解決事項の
節番号を1つずつ繰り下げたうえで、内容を都度棚卸しする運用が有効です）。